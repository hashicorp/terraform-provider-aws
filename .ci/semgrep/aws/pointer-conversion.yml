# Copyright IBM Corp. 2014, 2026
# "SPDX-License-Identifier: MPL-2.0"

# Generated by internal/generate/semgrep/pointer_conversion/main.go; DO NOT EDIT.

rules:
  - id: pointer-conversion-dereference-and-wrap
    fix: $VALUE
    languages: [go]
    message: Using AWS SDK for Go pointer conversion functions on dereferenced pointer values is extraneous
    patterns:
      - pattern-either:
          - pattern: aws.Bool(*$VALUE)
          - pattern: aws.Float32(*$VALUE)
          - pattern: aws.Float64(*$VALUE)
          - pattern: aws.Int32(*$VALUE)
          - pattern: aws.Int64(*$VALUE)
          - pattern: aws.String(*$VALUE)
          - pattern: aws.Time(*$VALUE)
    severity: WARNING

  - id: pointer-conversion-wrap-and-dereference
    fix: $VALUE
    languages: [go]
    message: Using AWS Go SDK pointer conversion, e.g. aws.String(), with immediate dereferencing is extraneous
    paths:
      include:
        - "/internal"
    patterns:
      - pattern-either:
          - pattern: "*aws.Bool($VALUE)"
          - pattern: "*aws.Float32($VALUE)"
          - pattern: "*aws.Float64($VALUE)"
          - pattern: "*aws.Int32($VALUE)"
          - pattern: "*aws.Int64($VALUE)"
          - pattern: "*aws.String($VALUE)"
          - pattern: "*aws.Time($VALUE)"
    severity: WARNING

  - id: pointer-conversion-ResourceData-SetId
    fix: d.SetId(aws.ToString($VALUE))
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion aws.ToString() function for dereferencing during d.SetId()
    pattern: "d.SetId(*$VALUE)"
    severity: WARNING

  - id: int64-pointer-dereference-and-convert
    fix: int(aws.ToInt64($VALUE))
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing when converting int64 to int
    pattern: int(*$VALUE)
    severity: WARNING

  - id: pointer-conversion-on-assignment
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing during assignment
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
        - "/internal/service/*/service_package.go"
        - "/internal/service/*/service_package_gen.go"
    patterns:
      - pattern: "$LHS = *$RHS"
      - pattern-not: "*$LHS2 = *$RHS"
    severity: WARNING

  - id: pointer-conversion-conditional-bool
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing in conditionals
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
    patterns:
      - pattern-either:
          - pattern: "($LHS : bool) == *$RHS"
          - pattern: "($LHS : bool) != *$RHS"
          - pattern: "*$LHS == ($RHS : bool)"
          - pattern: "*$LHS != ($RHS : bool)"
    severity: WARNING

  - id: pointer-conversion-conditional-float32
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing in conditionals
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
    patterns:
      - pattern-either:
          - pattern: "($LHS : float32) == *$RHS"
          - pattern: "($LHS : float32) != *$RHS"
          - pattern: "($LHS : float32) > *$RHS"
          - pattern: "($LHS : float32) < *$RHS"
          - pattern: "($LHS : float32) >= *$RHS"
          - pattern: "($LHS : float32) <= *$RHS"
          - pattern: "*$LHS == ($RHS : float32)"
          - pattern: "*$LHS != ($RHS : float32)"
          - pattern: "*$LHS > ($RHS : float32)"
          - pattern: "*$LHS < ($RHS : float32)"
          - pattern: "*$LHS >= ($RHS : float32)"
          - pattern: "*$LHS <= ($RHS : float32)"
    severity: WARNING

  - id: pointer-conversion-conditional-float64
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing in conditionals
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
    patterns:
      - pattern-either:
          - pattern: "($LHS : float64) == *$RHS"
          - pattern: "($LHS : float64) != *$RHS"
          - pattern: "($LHS : float64) > *$RHS"
          - pattern: "($LHS : float64) < *$RHS"
          - pattern: "($LHS : float64) >= *$RHS"
          - pattern: "($LHS : float64) <= *$RHS"
          - pattern: "*$LHS == ($RHS : float64)"
          - pattern: "*$LHS != ($RHS : float64)"
          - pattern: "*$LHS > ($RHS : float64)"
          - pattern: "*$LHS < ($RHS : float64)"
          - pattern: "*$LHS >= ($RHS : float64)"
          - pattern: "*$LHS <= ($RHS : float64)"
    severity: WARNING

  - id: pointer-conversion-conditional-int32
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing in conditionals
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
    patterns:
      - pattern-either:
          - pattern: "($LHS : int32) == *$RHS"
          - pattern: "($LHS : int32) != *$RHS"
          - pattern: "($LHS : int32) > *$RHS"
          - pattern: "($LHS : int32) < *$RHS"
          - pattern: "($LHS : int32) >= *$RHS"
          - pattern: "($LHS : int32) <= *$RHS"
          - pattern: "*$LHS == ($RHS : int32)"
          - pattern: "*$LHS != ($RHS : int32)"
          - pattern: "*$LHS > ($RHS : int32)"
          - pattern: "*$LHS < ($RHS : int32)"
          - pattern: "*$LHS >= ($RHS : int32)"
          - pattern: "*$LHS <= ($RHS : int32)"
    severity: WARNING

  - id: pointer-conversion-conditional-int64
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing in conditionals
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
    patterns:
      - pattern-either:
          - pattern: "($LHS : int64) == *$RHS"
          - pattern: "($LHS : int64) != *$RHS"
          - pattern: "($LHS : int64) > *$RHS"
          - pattern: "($LHS : int64) < *$RHS"
          - pattern: "($LHS : int64) >= *$RHS"
          - pattern: "($LHS : int64) <= *$RHS"
          - pattern: "*$LHS == ($RHS : int64)"
          - pattern: "*$LHS != ($RHS : int64)"
          - pattern: "*$LHS > ($RHS : int64)"
          - pattern: "*$LHS < ($RHS : int64)"
          - pattern: "*$LHS >= ($RHS : int64)"
          - pattern: "*$LHS <= ($RHS : int64)"
    severity: WARNING

  - id: pointer-conversion-conditional-string
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing in conditionals
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
    patterns:
      - pattern-either:
          - pattern: "($LHS : string) == *$RHS"
          - pattern: "($LHS : string) != *$RHS"
          - pattern: "($LHS : string) > *$RHS"
          - pattern: "($LHS : string) < *$RHS"
          - pattern: "($LHS : string) >= *$RHS"
          - pattern: "($LHS : string) <= *$RHS"
          - pattern: "*$LHS == ($RHS : string)"
          - pattern: "*$LHS != ($RHS : string)"
          - pattern: "*$LHS > ($RHS : string)"
          - pattern: "*$LHS < ($RHS : string)"
          - pattern: "*$LHS >= ($RHS : string)"
          - pattern: "*$LHS <= ($RHS : string)"
    severity: WARNING

  - id: pointer-conversion-conditional-time
    languages: [go]
    message: Prefer AWS Go SDK pointer conversion functions for dereferencing in conditionals
    paths:
      include:
        - "/internal/service"
      exclude:
        - "/internal/service/**/*_test.go"
    patterns:
      - pattern-either:
          - pattern: "($LHS : time.Time) == *$RHS"
          - pattern: "($LHS : time.Time) != *$RHS"
          - pattern: "($LHS : time.Time) > *$RHS"
          - pattern: "($LHS : time.Time) < *$RHS"
          - pattern: "($LHS : time.Time) >= *$RHS"
          - pattern: "($LHS : time.Time) <= *$RHS"
          - pattern: "*$LHS == ($RHS : time.Time)"
          - pattern: "*$LHS != ($RHS : time.Time)"
          - pattern: "*$LHS > ($RHS : time.Time)"
          - pattern: "*$LHS < ($RHS : time.Time)"
          - pattern: "*$LHS >= ($RHS : time.Time)"
          - pattern: "*$LHS <= ($RHS : time.Time)"
    severity: WARNING
