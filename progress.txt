## Codebase Patterns
- RDS proxy resources use `@SDKResource` annotation (SDK V2 pattern), not Framework
- Proxy resources reuse `findDBProxyByName()`, `waitDBProxyUpdated()` from proxy.go
- Composite resource IDs use `/` separator with `SplitN` parsing (see proxy_target.go)
- Auth configs on proxy use `UserAuthConfig` (input) vs `UserAuthConfigInfo` (output) types
- Export test vars in `exports_test.go` alphabetically within sections
- `make gen` must be run after adding `@SDKResource` annotations to register resources
- `validIdentifier` is the standard validator for db_proxy_name fields
- `verify.ValidARN` is used for ARN validation
- Cross-service client access works from any package: `meta.(*conns.AWSClient).IAMClient(ctx)`
- IAM SDK types alias: `awsiam "github.com/aws/aws-sdk-go-v2/service/iam/types"` avoids RDS types conflict
- `tfresource.NewEmptyResultError()` takes no arguments
- IAM policy documents come URL-encoded from AWS; use `url.QueryUnescape()` then `verify.PolicyToSet()`
- Cross-service test helpers: use `acctest.ProviderMeta(ctx, t).IAMClient(ctx)` for IAM in RDS tests

---

## 2026-03-19 - US-001
- Implemented aws_db_proxy_secret resource skeleton with full CRUD + Import
- Files changed: internal/service/rds/proxy_secret.go (new), internal/service/rds/exports_test.go, internal/service/rds/service_package_gen.go (generated)
- **Learnings for future iterations:**
  - The resource actually implements full CRUD (not just stubs) since the create/read/delete logic for proxy secrets is straightforward (ModifyDBProxy to add/remove secret from auth list)
  - Import uses `db_proxy_name/secret_arn` format parsed with SplitN(id, "/", 2)
  - Auth config conversion from `UserAuthConfigInfo` (read response) to `UserAuthConfig` (modify input) requires manually mapping fields
  - `go build ./internal/service/rds/...` takes ~2 minutes on this machine
---

## 2026-03-19 - US-002
- No code changes needed — Create and Delete were fully implemented in US-001
- All acceptance criteria verified: ModifyDBProxy calls, waiters, error handling, compilation, vet
- **Learnings for future iterations:**
  - When US-001 implements full CRUD instead of stubs, subsequent stories for the same operations can be marked as passing without code changes
---

## 2026-03-19 - US-003
- No code changes needed — Read and Import were fully implemented in US-001
- All acceptance criteria verified: DescribeDBProxies via findDBProxyByName, state removal on missing secret, import with db_proxy_name/secret_arn format
---

## 2026-03-19 - US-004, US-005, US-006
- Implemented aws_db_proxy_policy_attachment resource with full CRUD + Import
- Files changed: internal/service/rds/proxy_policy_attachment.go (new), internal/service/rds/exports_test.go, internal/service/rds/service_package_gen.go (generated)
- **Learnings for future iterations:**
  - Cross-service client access: `meta.(*conns.AWSClient).IAMClient(ctx)` works from any service package
  - IAM role policy uses inline pattern (PutRolePolicy/GetRolePolicy/DeleteRolePolicy)
  - IAM SDK types need alias import: `awsiam "github.com/aws/aws-sdk-go-v2/service/iam/types"` to avoid conflict with RDS types
  - Proxy RoleArn must be parsed with `arn.Parse()` then extract role name from Resource field (`role/path/name`)
  - `tfresource.NewEmptyResultError()` takes no arguments in this codebase
  - Policy normalization uses `structure.NormalizeJsonString()` and `verify.PolicyToSet()` for read
  - Policy document comes URL-encoded from AWS API, must `url.QueryUnescape()` it
---

## 2026-03-19 - US-007
- Implemented acceptance tests for aws_db_proxy_secret: basic (with import step) and disappears
- Files changed: internal/service/rds/proxy_secret_test.go (new)
- **Learnings for future iterations:**
  - Proxy secret tests need a second secret (additional) separate from the proxy's initial auth secret
  - Use `acctest.RandomWithPrefix(t, acctest.ResourcePrefix)` (not sdkacctest) for random names
  - Use `acctest.ParallelTest(ctx, t, ...)` not `resource.ParallelTest()`
  - Exists helper checks proxy's auth list for the specific secret ARN via attributes
  - Destroy helper iterates `s.RootModule().Resources`, filters by type, uses attributes to verify
  - `testAccDBProxyPreCheck` is defined in proxy_test.go and reusable across proxy sub-resource tests
---

## 2026-03-19 - US-008
- Implemented acceptance tests for aws_db_proxy_policy_attachment: basic (with import), disappears, and update
- Files changed: internal/service/rds/proxy_policy_attachment_test.go (new), internal/service/rds/exports_test.go (added FindDBProxyRoleName, FindRolePolicyForProxy exports)
- **Learnings for future iterations:**
  - Cross-service test helpers need IAM client: `acctest.ProviderMeta(ctx, t).IAMClient(ctx)`
  - Policy attachment exists/destroy checks need both RDS client (to find role name) and IAM client (to check policy)
  - Update test for policy_document changes the Action list (add DescribeSecret) to verify in-place update works
  - Reuse testAccProxyConfig_base() from proxy_test.go as foundation for proxy sub-resource test configs
---

## 2026-03-19 - US-009
- Created documentation for aws_db_proxy_secret resource
- Files changed: website/docs/r/db_proxy_secret.html.markdown (new)
- **Learnings for future iterations:**
  - Follow the format of website/docs/r/db_proxy_target.html.markdown for sub-resource docs
  - Include frontmatter with subcategory "RDS (Relational Database)", layout "aws", page_title, and description
  - Import section should show both `import` block (v1.5.0+) and `terraform import` CLI examples
  - `make docs-lint-fix` and `make website-lint-fix` require `misspell` tool which may not be installed locally
  - Timeouts section should match the resource's actual timeout configuration
---

## 2026-03-19 - US-010
- Created documentation for aws_db_proxy_policy_attachment resource
- Files changed: website/docs/r/db_proxy_policy_attachment.html.markdown (new)
- **Learnings for future iterations:**
  - Policy attachment docs should explain the inline IAM policy pattern (PutRolePolicy on proxy's execution role)
  - Timeouts for this resource are 5m (not 30m like the proxy secret), matching the resource schema
  - Example usage should show a realistic IAM policy (e.g., secretsmanager:GetSecretValue) since that's the primary use case
---

## 2026-03-19 - US-011
- Created changelog entry in .changelog/00000.txt with release-note:new-resource entries for both aws_db_proxy_secret and aws_db_proxy_policy_attachment
- Files changed: .changelog/00000.txt (new)
- **Learnings for future iterations:**
  - Changelog files are named by PR number; use 00000.txt as placeholder when PR number is unknown
  - Format is triple-backtick fenced blocks with `release-note:new-resource` header, one resource name per block
  - No additional description needed for new resources (just the resource name)
---
