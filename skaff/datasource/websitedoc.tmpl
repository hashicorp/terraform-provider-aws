---
subcategory: "{{ .HumanFriendly }}"
layout: "aws"
page_title: "AWS: aws_{{ .ServicePackage }}_{{ .DataSourceSnake }}"
description: |-
  Terraform data source for managing an AWS {{ .HumanFriendly }} {{ .HumanDataSourceName }}.
---

# Data Source: aws_{{ .ServicePackage }}_{{ .DataSourceSnake }}

Terraform data source for managing an AWS {{ .HumanFriendly }} {{ .HumanDataSourceName }}.

## Example Usage

### Basic Usage

```terraform
data "aws_{{ .ServicePackage }}_{{ .DataSourceSnake }}" "example" {
}
```

## Argument Reference

The following arguments are required:

* `example_arg` - (Required) Concise argument description.

The following arguments are optional:

* `optional_arg` - (Optional) Concise argument description.

## Attributes Reference

In addition to all arguments above, the following attributes are exported:

* `arn` - ARN of the {{ .HumanDataSourceName }}.
* `example_attribute` - Concise description.
