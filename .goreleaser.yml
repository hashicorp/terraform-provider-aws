# Build customization
builds:
  - binary: terraform-provider-aws
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64

# Archive customization
archives:
  - format: zip

    replacements:
      amd64: 64-bits
      386: 32-bits
      darwin: macOS

    files:
      - nothing.*
