```release-note:bug
resource/aws_elasticache_replication_group: Fixed an issue with migration of `aws_elasticache_replication_group`. Previously, `aws_elasticache_replication_group` with `engine=valkey` and `engine_version=7.2` used to get replaced if `engine` was set to `redis` and `engine_version` was set to `7.1`. This is now fixed, after this fix terraform will perform in-place update.
```