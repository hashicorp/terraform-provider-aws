```release-note:bug
resource/aws_db_instance: Respect an explicit `dedicated_log_volume = false` when creating a read replica, restoring from a snapshot, or restoring to a point-in-time. Previously the setting was silently ignored, causing the replica/restored instance to inherit the source's dedicated log volume.
```
