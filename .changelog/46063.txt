```release-note:enhancement
resource/aws_servicequotas_service_quota: Add `wait_for_fulfillment` argument to wait for quota increase request approval and for the desired quota value to be enacted before completing resource creation or update. Reuse an already-open request, reject approvals below the requested value, and ensure the configured timeout covers both wait phases. Support the edge case where the desired value is already satisfied to reduce request churn. Also supports custom timeouts for requests that take longer than the default (10m) to pass through.
```
