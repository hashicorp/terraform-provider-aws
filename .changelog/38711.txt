```release-note:bug
resource/aws_s3_bucket_lifecycle_configuration: Make `filter.and.object_size_greater_than` and `filter.and.object_size_less_than` fields more consistent in type as well as flatten/expand behavior to the `filter.object_size_greater_than` and `filter.object_size_less_than` fields.
```